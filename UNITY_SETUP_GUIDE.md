# Unity Setup Guide: Battle Scene & UI Configuration

เอกสารนี้จะอธิบายขั้นตอนการ Setup Scene และ UI ใน Unity Editor เพื่อให้ระบบ Gameplay (Logic) ที่เขียนไปแล้วสามารถทำงานและแสดงผลได้จริง

---

## 🛠️ Step 1: Prepare Prefabs (เตรียมแม่พิมพ์)

ก่อนเริ่มจัดหน้าจอ เราต้องสร้าง Prefab ของ UI Element ย่อยๆ เตรียมไว้ก่อน

### 1.1 Unit HUD Prefab (แถบสถานะตัวละคร)
1.  ใน Hierarchy คลิกขวา `UI > Panel`. ตั้งชื่อ **`UnitHUD_Prefab`**.
2.  ปรับขนาดให้พอดีกับกรอบตัวละคร (เช่น Width: 200, Height: 300).
3.  **ส่วนประกอบภายใน:**
    *   **Name Text:** `UI > Legacy > Text`. ตั้งชื่อ `NameText`.
    *   **HP Bar:** `UI > Slider`. ตั้งชื่อ `HPSlider`. (ลบ Handle Slide Area ทิ้งได้ถ้าไม่ใช้).
    *   **Sanity Bar:** `UI > Slider`. ตั้งชื่อ `SanitySlider`. เปลี่ยนสี Fill เป็นสีฟ้า (Lucid).
    *   **Shield Text:** `UI > Legacy > Text`. ตั้งชื่อ `ShieldText`.
    *   **Active Indicator:** `UI > Image`. ตั้งชื่อ `ActiveHighlight`. (ทำเป็นกรอบสีเหลืองรอบๆ เอาไว้โชว์ว่าใครถูกเลือก).
4.  **Attach Script:**
    *   ลาก Script `UnitStatusUI.cs` (จากโฟลเดอร์ UI) มาใส่ที่ **`UnitHUD_Prefab`**.
    *   ลาก Component ที่สร้างในข้อ 3 เข้าช่อง Reference ใน Inspector ให้ครบ:
        *   Name Text -> `Name Text`
        *   Hp Slider -> `Hp Slider`
        *   Active Turn Indicator -> `ActiveHighlight`
        *   Sanity Fill Image -> `SanitySlider/Fill Area/Fill` (ตัว Image ที่เปลี่ยนสีได้)
5.  ลาก **`UnitHUD_Prefab`** จาก Hierarchy ลงมาที่ Project Folder เพื่อสร้างเป็น Prefab. ลบตัวใน Scene ทิ้งได้.

### 1.2 Card Prefab (การ์ด)
1.  ใน Hierarchy คลิกขวา `UI > Button` (หรือ Image). ตั้งชื่อ **`Card_Prefab`**.
2.  ปรับขนาด (เช่น Width: 150, Height: 220).
3.  **ส่วนประกอบภายใน:**
    *   **Name:** `UI > Legacy > Text`.
    *   **Cost:** `UI > Legacy > Text` (มุมซ้ายบน).
    *   **Description:** `UI > Legacy > Text`.
    *   **Artwork:** `UI > Image`.
    *   **Lock Button:** `UI > Button` (เล็กๆ มุมขวาบน). ตั้งชื่อ `LockButton`. ใส่ `Image` รูปแม่กุญแจ (ถ้ามี) หรือใช้สีแดงแทน.
    *   **Lock Overlay:** `UI > Image`. (เอาไว้ซ้อนทับการ์ดเวลา Lock ปรับ Alpha ให้จางๆ) ตั้งชื่อ `LockIcon`.
4.  **Attach Script:**
    *   ลาก Script `CardUI.cs` มาใส่ที่ **`Card_Prefab`**.
    *   ลาก Reference เข้าช่อง:
        *   Card Button -> ตัว `Card_Prefab` เอง (ที่มี Button Component)
        *   Lock Button -> `LockButton`
        *   Lock Icon -> `LockIcon`
        *   Text ต่างๆ -> ตามที่สร้าง
5.  ลาก **`Card_Prefab`** ลง Project Folder เป็น Prefab.

### 1.3 Command Slot Prefab (ช่องคิวคำสั่ง)
1.  คลิกขวา `UI > Image`. ตั้งชื่อ **`CmdSlot_Prefab`**.
2.  ปรับขนาดเล็กๆ (50x50).
3.  ใส่ `UI > Legacy > Text` ตรงกลาง ไว้โชว์ตัวอักษร (ATK/DEF).
4.  ลากลง Project Folder เป็น Prefab.

---

## 🎬 Step 2: Battle Scene Setup (จัดฉากต่อสู้)

### 2.1 Battle Managers (ระบบเบื้องหลัง)
1.  สร้าง Empty GameObject ตั้งชื่อ **`--- MANAGERS ---`**.
2.  สร้าง Empty GameObject ลูก ตั้งชื่อ **`BattleSystem`**.
    *   Add Component: **`BattleManager`**.
    *   Add Component: **`DeckManager`**.
    *   Add Component: **`TimelineManager`**.
    *   Add Component: **`FieldManager`**.
    *   Add Component: **`ComboResolver`**.
3.  **Combo Resolver Setup:**
    *   ถ้ามีไฟล์ `ComboDefinition` (ScriptableObject) ที่สร้างไว้ ให้ลากใส่ List ใน ComboResolver (ถ้าไม่มีไม่เป็นไร มันมี Basic Combo อยู่แล้ว).

### 2.2 Units (ตัวละครในฉาก)
1.  สร้าง Empty GameObject ตั้งชื่อ **`--- UNITS ---`**.
2.  **Player Party:**
    *   สร้าง Cube หรือ Sprite (2D Object) ตั้งชื่อ **`Hero_1`**.
    *   Add Component: **`Unit`**.
    *   **ตั้งค่า Inspector:**
        *   `Is Player`: **CHECK (✓)**
        *   `Unit Name`: "Naoto"
        *   `Max HP`: 1000
        *   `Agility`: 120
    *   ทำซ้ำให้ได้ 3 ตัว (`Hero_2`, `Hero_3`) (อย่าลืมติ๊ก Is Player ทุกตัว).
3.  **Boss:**
    *   สร้าง Cube/Sprite ใหญ่หน่อย ตั้งชื่อ **`Boss`**.
    *   Add Component: **`BossUnit`**.
    *   **ตั้งค่า Inspector:**
        *   `Is Player`: **UNCHECK (X)**
        *   `Unit Name`: "The Paradox"
        *   `Phase Health Pools`: ใส่เลขลงไป เช่น Element 0 = 5000, Element 1 = 5000 (2 Phases).

---

## 🖥️ Step 3: UI Layout & Wiring (เชื่อมต่อหน้าจอ)

### 3.1 Main Canvas
1.  ใน Hierarchy คลิกขวา `UI > Canvas`. ตั้งชื่อ **`BattleCanvas`**.
2.  ตั้งค่า `Canvas Scaler`: UI Scale Mode = **Scale With Screen Size** (Reference: 1920 x 1080).

### 3.2 Battle UI Manager
1.  สร้าง Empty GameObject ใต้ Canvas ตั้งชื่อ **`BattleUI_Manager`**.
2.  Add Component: **`BattleUI`**. (นี่คือสมองหลักของ UI).

### 3.3 Party Panel (HUD ด้านบน/ข้าง)
1.  สร้าง `UI > Panel` ใต้ Canvas. ตั้งชื่อ **`PartyPanel`**.
2.  ปรับตำแหน่งไปไว้มุมซ้ายล่าง หรือด้านล่าง (พื้นที่สำหรับ HUD ตัวละคร).
3.  Add Component: **`Horizontal Layout Group`** (เพื่อให้ HUD เรียงกันสวยๆ).
    *   Spacing: 10
    *   Child Control Size: Width/Height (ติ๊กถูก).

### 3.4 Hand Panel (ที่อยู่การ์ด)
1.  สร้าง `UI > Panel` ใต้ Canvas. ตั้งชื่อ **`HandPanel`**.
2.  ปรับตำแหน่งไปไว้ด้านล่างสุดกลางจอ.
3.  Add Component: **`HandUI`**.
    *   Add Component: **`Horizontal Layout Group`**.
4.  **Wiring:** ลาก `Card_Prefab` (จาก Step 1.2) มาใส่ช่อง `Card Prefab` ใน Inspector ของ `HandUI`.
5.  ลากช่อง `Hand Container` = ตัว `HandPanel` เอง.

### 3.5 Command Queue (คิวคำสั่ง)
1.  สร้าง `UI > Panel` ใต้ Canvas. ตั้งชื่อ **`CommandQueuePanel`**.
2.  ปรับตำแหน่งไว้เหนือ HandPanel หรือใกล้ๆ PartyPanel.
3.  Add Component: **`CommandQueueUI`**.
    *   Add Component: **`Horizontal Layout Group`**.
4.  **Wiring:** ลาก `CmdSlot_Prefab` (จาก Step 1.3) มาใส่ช่อง `Slot Prefab`.
5.  ลากช่อง `Slots Container` = ตัว `CommandQueuePanel` เอง.

---

## 🔌 Step 4: Final Wiring (เชื่อมสายครั้งสุดท้าย)

กลับไปที่ **`BattleUI_Manager`** (จาก Step 3.2) แล้วลาก Reference ให้ครบ:

*   **Party Panel Container:** ลาก `PartyPanel` (Step 3.3) มาใส่.
*   **Unit Status Prefab:** ลาก `UnitHUD_Prefab` (Step 1.1) มาใส่.
*   **Command Queue UI:** ลาก `CommandQueuePanel` (Step 3.5) มาใส่.
*   **Hand UI:** ลาก `HandPanel` (Step 3.4) มาใส่.

---

## ✅ Step 5: Test Run
1.  กด **Play** ใน Unity Editor.
2.  **สิ่งที่ควรเกิดขึ้น:**
    *   `UnitHUD` ควรเด้งขึ้นมา 3 อัน ตามจำนวน Hero ที่สร้างไว้.
    *   มี Active Highlight ที่ Hero ตัวแรก.
    *   การ์ด 5 ใบควรถูกจั่วขึ้นมาบน `HandPanel`.
3.  **การควบคุม:**
    *   กด **Tab**: เปลี่ยนตัวละครที่ Active (Highlight เปลี่ยน).
    *   คลิก **การ์ด**: การ์ดหายไป (ถูกใช้) -> Command Icon (ATK/DEF/ANA) ขึ้นที่ `CommandQueue`.
    *   คลิก **ปุ่ม Lock บนการ์ด**: ไอคอนกุญแจขึ้น.
    *   กด **Spacebar**: จบเทิร์น -> เข้าสู่ Execution Phase (ดู Log ใน Console).
    *   เริ่มเทิร์นใหม่: การ์ดที่ไม่ได้ Lock จะหายไป การ์ดใหม่จะมาแทน.

---

**Tip:** ถ้า UI ไม่ขึ้น หรือ Error ให้ดู Console แล้วเช็คว่าลาก Reference ครบหรือยัง ส่วนใหญ่มักจะลืมลาก Prefab หรือ Container ครับ.

